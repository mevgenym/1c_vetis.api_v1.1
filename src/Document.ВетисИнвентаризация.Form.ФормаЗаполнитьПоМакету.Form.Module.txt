
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	//Таблица.Вывести(Документы.ВетисИнвентаризация.ПолучитьМакет("Макет"));
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаГотово(Команда)
	
	_список = Новый Массив;
	
	Для _НомерСтроки = 2 По Таблица.ВысотаТаблицы Цикл
		_Строка = Новый Структура;
		_Строка.Вставить("Количество", Число(Таблица.Область(_НомерСтроки, 1).Текст));
		_Строка.Вставить("ProductItemName", Таблица.Область(_НомерСтроки, 2).Текст);
		_список.Добавить(_Строка);
	КонецЦикла;
	
	Закрыть(_список);
КонецПроцедуры
