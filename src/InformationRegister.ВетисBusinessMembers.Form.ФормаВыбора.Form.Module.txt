
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	//Если Параметры.Свойство("BusinessEntity") Тогда
	//	//ВетисОбщегоНазначения.ДобавитьОтбор(Список.Отбор, "BusinessEntity", Параметры.BusinessEntity);
	//КонецЕсли;
	
	//Если Параметры.Свойство("Enterprise") Тогда
	//	//ВетисОбщегоНазначения.ДобавитьОтбор(Список.Отбор, "Enterprise", Параметры.Enterprise);
	//КонецЕсли;
	
	Если Параметры.Свойство("BusinessEntity") И Параметры.Свойство("Enterprise") Тогда
		Элементы.Список.ТекущаяСтрока = РегистрыСведений.ВетисBusinessMembers.СоздатьКлючЗаписи(Новый Структура("BusinessEntity,Enterprise", Параметры.BusinessEntity, Параметры.Enterprise));
	КонецЕсли;
	
	ВетисОбщегоНазначения.ДобавитьОтбор(Список.Отбор, "Свой", Истина);
	ВетисОбщегоНазначения.ДобавитьОтбор(Список.Отбор, "Использовать", Истина);
	
КонецПроцедуры
