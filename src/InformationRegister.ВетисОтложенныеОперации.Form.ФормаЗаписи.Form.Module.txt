
&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
	
	Сообщение = ТекущийОбъект.Сообщение.Получить();
	
	Если ПустаяСтрока(Сообщение) Тогда
		Сообщение = ТекущийОбъект.ПредставлениеСообщения;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если НЕ ЗначениеЗаполнено(Запись.ИсходныйКлючЗаписи.УникальныйИдентификатор) Тогда
		Запись.Ошибка = Ложь;
		Запись.Завершено = Ложь;
		Запись.УникальныйИдентификатор = Неопределено;
		Сообщение = "";
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	
	ТекущийОбъект.ПредставлениеСообщения = Сообщение;
	
	ТекущийОбъект.Сообщение = Новый ХранилищеЗначения(Сообщение);
	
КонецПроцедуры
