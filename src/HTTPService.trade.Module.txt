
Функция QueryGET(Запрос)
	
	КодСостояния = 200;
	
	Ответ = "It works!";
	
	HTTPОтвет = Новый HTTPСервисОтвет(КодСостояния);
	HTTPОтвет.УстановитьТелоИзСтроки(Ответ);
	
	Возврат HTTPОтвет;
	
КонецФункции

Функция QueryPOST(Запрос)
	
	КодСостояния = 200;
	
	Ответ = ВетисСервис.Синхронизировать(Запрос);
	
	HTTPОтвет = Новый HTTPСервисОтвет(КодСостояния);
	HTTPОтвет.УстановитьТелоИзСтроки(Ответ);
	
	Возврат HTTPОтвет;
	
КонецФункции
