
Функция QueryGET(Запрос)
	
	КодСостояния = 200;
	
	Ответ = ВетисСервис.ОбработатьВходящийЗапрос(Запрос);
	
	HTTPОтвет = Новый HTTPСервисОтвет(КодСостояния);
	HTTPОтвет.УстановитьТелоИзСтроки(Ответ);
	
	Возврат HTTPОтвет;
	
КонецФункции

Функция QueryPOST(Запрос)
	
	КодСостояния = 200;
	
	Ответ = ВетисСервис.ОбработатьВходящийЗапрос(Запрос);
	
	HTTPОтвет = Новый HTTPСервисОтвет(КодСостояния);
	HTTPОтвет.УстановитьТелоИзСтроки(Ответ);
	
	Возврат HTTPОтвет;
	
КонецФункции
