
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
КонецПроцедуры

&НаКлиенте
Процедура Заполнить(Команда)
	
	ВетисСинхронизацияВызовСервера.PackingForm();
	
	Элементы.Список.Обновить();
	
КонецПроцедуры

&НаСервере
Процедура Команда1НаСервере()
	
	//Запрос = Новый Запрос(
	//"ВЫБРАТЬ
	//|	ВетисPackingForm.Ссылка,
	//|	ВетисPackingForm.guid
	//|ИЗ
	//|	Справочник.ВетисPackingForm КАК ВетисPackingForm");
	//
	//Запрос.УстановитьПараметр("", );
	//
	//Результат = Запрос.Выполнить();
	//Выборка = Результат.Выбрать();
	//
	//Пока Выборка.Следующий() Цикл
	//	_Объект = Выборка.Ссылка.ПолучитьОбъект();
	//	_Объект.guid = Строка(Выборка.Ссылка.УникальныйИдентификатор());
	//	_Объект.Записать();
	//КонецЦикла;
	
	
КонецПроцедуры

&НаКлиенте
Процедура Команда1(Команда)
	Команда1НаСервере();
КонецПроцедуры
