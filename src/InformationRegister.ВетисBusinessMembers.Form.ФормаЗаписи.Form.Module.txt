
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
КонецПроцедуры


&НаКлиенте
Процедура КомандаОбновитьСвязь(Команда)
	
	КомандаОбновитьСвязьНаСервере();
	
КонецПроцедуры


&НаСервере
Процедура КомандаОбновитьСвязьНаСервере()
	
	Перем _Отказ, _Ошибка;
	
	_BusinessEntityGuid = ВетисDictionaryСлой1с.BusinessEntity(Запись.BusinessEntity);
	
	_EnterpriseGuid = ВетисDictionaryСлой1с.Enterprise(Запись.Enterprise);
	
	_Enterprise = Неопределено;
	
	_параметры = Неопределено;
	
	_Версия20 = Ветис.Версия_2_0();
	
	Пока ?(_Версия20, ВетисEnterpriseService_2_0, ВетисEnterpriseService).GetActivityLocationListNext(_BusinessEntityGuid, _Enterprise, _параметры, _Отказ, _Ошибка) Цикл
		Если _Отказ = Истина Тогда
			Прервать;
		ИначеЕсли _EnterpriseGuid = _Enterprise.guid Тогда
			Запись.ВСервисе = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры
