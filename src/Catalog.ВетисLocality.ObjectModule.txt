
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	Если ТипЗнч(ДанныеЗаполнения) = Тип("ОбъектXDTO") Тогда
		ПометкаУдаления = Ложь;
		guid         = ДанныеЗаполнения.guid;
		last         = ДанныеЗаполнения.last;
		active       = ДанныеЗаполнения.active;
		status       = ВетисBaseСлой1с.VersionStatus(ДанныеЗаполнения.Status);
		Наименование = СокрЛП(ДанныеЗаполнения.name);
		country      = ВетисDictionaryСлой1с.Country(ДанныеЗаполнения.countryGuid);
		region       = ВетисDictionaryСлой1с.Region(ДанныеЗаполнения.regionGuid);
		district     = ВетисDictionaryСлой1с.District(ДанныеЗаполнения.districtGuid);
		view         = СокрЛП(ДанныеЗаполнения.view);
	КонецЕсли;
	
КонецПроцедуры
