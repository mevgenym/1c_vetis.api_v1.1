
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	_Запись = РеквизитФормыВЗначение("Запись");
	
	original = _Запись.original.Получить();
	
	work = _Запись.work.Получить();
	
КонецПроцедуры

&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	
	ТекущийОбъект.original = Новый ХранилищеЗначения(original, Новый СжатиеДанных(9));
	
	ТекущийОбъект.work = Новый ХранилищеЗначения(work, Новый СжатиеДанных(9));
	
КонецПроцедуры
