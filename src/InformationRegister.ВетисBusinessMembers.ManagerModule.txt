
Процедура Добавить(BusinessEntity, Enterprise, пСвой = Неопределено) Экспорт
	
	_мз = РегистрыСведений.ВетисBusinessMembers.СоздатьМенеджерЗаписи();
	_мз.BusinessEntity = BusinessEntity;
	_мз.Enterprise = Enterprise;
	
	_мз.Прочитать();
	
	_мз.BusinessEntity = BusinessEntity;
	_мз.Enterprise = Enterprise;
	_мз.Использовать = Истина;
	_мз.Свой = ?(пСвой = Неопределено, _мз.Свой, пСвой);
	_мз.Записать();
	
	//_нз = РегистрыСведений.ВетисBusinessMembers.СоздатьНаборЗаписей();
	//
	//Если пОбменДаннымиЗагрузка = Истина Тогда
	//	_нз.ОбменДанными.Загрузка = Истина;
	//КонецЕсли;
	//
	//_нз.Отбор.BusinessEntity.Установить(пХС);
	//_нз.Отбор.Enterprise.Установить(пПредприятие);
	//
	//_нз.Прочитать();
	//
	//Если _нз.Количество() = 0 Тогда
	//	_мз = _нз.Добавить();
	//	_мз.Использовать = Истина;
	//Иначе
	//	_мз = _нз.Получить(0);
	//КонецЕсли;
	//
	//_мз.BusinessEntity = пХС;
	//
	//_мз.Enterprise   = пПредприятие;
	//
	//_нз.Записать();
	
КонецПроцедуры

Процедура Удалить(BusinessEntity, Enterprise) Экспорт
	
	_мз = РегистрыСведений.ВетисBusinessMembers.СоздатьМенеджерЗаписи();
	_мз.BusinessEntity = BusinessEntity;
	_мз.Enterprise = Enterprise;
	_мз.Удалить();
	
КонецПроцедуры
